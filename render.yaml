services:
  - type: web
    name: malaria-detection
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 5000
    healthCheckPath: /health
